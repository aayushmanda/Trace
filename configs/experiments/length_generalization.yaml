experiment: length_generalization
train_depth: 8
eval_depths: [8, 10, 12, 16]
modes: [outcome, process]
seeds: [2001, 2002, 2003]
train:
  train_size: 20000
  val_size: 500
  steps: 4000
  lr: 0.0003
  batch_size: 128
  compile: true
  bf16: true
model:
  embedding: 128
  heads: 4
  layers: 2
out: results/revision/length_generalization.csv
ckpt_dir: results/revision/length_ckpts
