# Experiment map (trimmed)

| Code | Command | Claim |
|---|---|---|
| Outcome-local mechanism | `python -m src outcome-local --smoke --use-nnsight` | Probe $H_t(h_t)\to s_t$ + causal patch |
| Rule credit table | `python experiments/build_rule_credit_table.py` | Table-space credit (App. L_diagnostics) |
| Executor comparison | `python -m src executor` | Trained executor bridge |

Removed from appendix: GSM8K playbook, alignment-unmeasured grids, unrun confirmation depths.
